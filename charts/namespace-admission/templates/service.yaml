apiVersion: v1
kind: Service
metadata:
  name: {{ include "namespace-admission.fullname" . }}
  labels:
    {{- include "namespace-admission.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "namespace-admission.selectorLabels" . | nindent 4 }}
