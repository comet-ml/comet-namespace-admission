apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namespace-admission.fullname" . }}-test-connection"
  labels:
    {{- include "namespace-admission.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "namespace-admission.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
